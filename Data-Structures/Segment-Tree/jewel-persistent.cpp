#include <bits/stdc++.h>
#define MAX 100005
using namespace std;
vector<int>tree[MAX<<2];
int main()
{
	int n;
	while(~scanf("%d", &n))
	{
		build(1, 1, n);
		for(int i = 0;i < n;i++)
		{
		
		}
	}
	return 0;
}
void build(int p, int l, int r)
{
	if(l == r)
		
}
